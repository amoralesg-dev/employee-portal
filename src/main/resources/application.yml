spring:
  application:
    name: employee-portal
  profiles:
    active: dev

app:
  security:
    jwt:
      secret: ${JWT_SECRET:CHANGE_ME_DEV_SECRET_CHANGE_ME_DEV_SECRET_123456789}
      expiration-minutes: ${JWT_EXPIRATION_MINUTES:60}

  # JPA / Hibernate (común)
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        physical_naming_strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    open-in-view: false

server:
  servlet:
    context-path: /employee-portal

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

# Springdoc / Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# Info del proyecto
info:
  app:
    name: Employee Portal
    description: API backend del Portal de Empleados
    version: 1.0.0
